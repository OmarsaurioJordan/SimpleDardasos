if(player.blanco>0 and instance_exists(player.blanco)){
    //camara.direction=point_direction(player.x,player.y,player.blanco.x,player.blanco.y);
    }
else{
    camara.direction=player.direction;}
camara.depth=visualizacion*0.3;
